# Root environment file for /srv/cs
# Copy to /srv/cs/.env and update the image registry values before deploying.

COMPOSE_PROJECT_NAME=cs
# Prefix for GHCR images (no tag)
MEDUSA_IMAGE=ghcr.io/<org>/cs-medusa
STRAPI_IMAGE=ghcr.io/<org>/cs-strapi
# Deployment automation overwrites TAG with the current commit SHA
TAG=latest
