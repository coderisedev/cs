{
  "framework": "nextjs",
  "installCommand": "(corepack pnpm install --frozen-lockfile || pnpm install --frozen-lockfile || npm install)",
  "buildCommand": "pnpm run build",
  "outputDirectory": ".vercel/output"
}
