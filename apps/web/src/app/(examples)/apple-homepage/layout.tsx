export const metadata = {
    title: 'Apple Style Homepage - Demo',
    description: 'Apple-style homepage powered by Strapi CMS',
};

export default function AppleHomepageLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
